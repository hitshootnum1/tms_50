<center><h1><%= t "user.all" %></h1></center>

<%= paginate @users %>
<ul class="users">
  <% if @users.any? %>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user %>
        <%= user.email %>
      </li>
      <hr>
    <% end %>
  <% end %>
</ul>
